//
// RADIX-SORT(A, d)
// 	for i = 1 to d
// 		use a stable sort to sort array A on digit i
//

package sortings

func Radix(numbers []int, d int) []int {
	if len(numbers) < 2 {
		return numbers
	}

	return numbers
}
